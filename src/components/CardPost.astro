---
interface Props {
    url: string;
    title: string;
    date: string;
}

const { url, title, date } = Astro.props;
---

<li class="card">
    <a class="card__link" href={url}>
        <h3 class="card__title">
            {title}
        </h3>
    </a>
</li>
